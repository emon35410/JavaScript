let age = 20;
const hashID = true;

console.log(age > 18 && hashID); // AND operation must be both are true
console.log(age > 18 || hashID); // OR operation at least one should be true
console.log(!hashID); // flip the value true will be false and false will be true